version: '3.8'

services:
  service:
    build: 
      context: ../../
      dockerfile: build/imgresizr/Dockerfile
    image: imgresizr:develop
    restart: always
    ports:
      - "80:9000"
#     networks:
#       - imgresizrnet
#   web:
#     image: nginx:latest
#     volumes:
#       - ./web:/usr/share/nginx/html
#       - ./conf/nginx.conf:/etc/nginx/conf.d/default.conf
#     ports:
#       - "80:80"
#     command: ['/bin/sh', '-c', 'nginx -g "daemon off;"']
#     restart: always
#     networks:
#       - imgresizrnet

networks:
   imgresizrnet:
